# Cursor Operating Guide

Cursor implements complete slices and reviews Codex slices. It uses OpenSpec, runs all checks,
prepares evidence, corrects review findings, handles deployment/human acceptance when required,
archives the change, regenerates context, and prepares the PR.
